---
const socialLinks = [
  {
    name: "GitHub",
    url: "https://github.com/PabloAML1",
    icon: `<svg viewBox="0 0 24 24" fill="currentColor" class="w-6 h-6"><path d="M12 0C5.37 0 0 5.37 0 12c0 5.31 3.435 9.795 8.205 11.385.6.105.825-.255.825-.57 0-.285-.015-1.23-.015-2.235-3.015.555-3.795-.735-4.035-1.41-.135-.345-.72-1.41-1.23-1.695-.42-.225-1.02-.78-.015-.795.945-.015 1.62.87 1.845 1.23 1.08 1.815 2.805 1.305 3.495.99.105-.78.42-1.305.765-1.605-2.67-.3-5.46-1.335-5.46-5.925 0-1.305.465-2.385 1.23-3.225-.12-.3-.54-1.53.12-3.18 0 0 1.005-.315 3.3 1.23.96-.27 1.98-.405 3-.405s2.04.135 3 .405c2.295-1.56 3.3-1.23 3.3-1.23.66 1.65.24 2.88.12 3.18.765.84 1.23 1.905 1.23 3.225 0 4.605-2.805 5.625-5.475 5.925.435.375.81 1.095.81 2.22 0 1.605-.015 2.895-.015 3.3 0 .315.225.69.825.57A12.02 12.02 0 0024 12c0-6.63-5.37-12-12-12z"/></svg>`,
  },
  {
    name: "LinkedIn",
    url: "https://www.linkedin.com/in/pablo-montero-lopez/",
    icon: `<svg viewBox="0 0 24 24" fill="currentColor" class="w-6 h-6"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>`,
  },
  {
    name: "Email",
    url: "mailto:monteropablo511@gmail.com",
    icon: `<svg viewBox="0 0 24 24" fill="currentColor" class="w-6 h-6"><path d="M20 4H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/></svg>`,
  },
  {
    name: "WhatsApp",
    url: "https://wa.me/593984398542",
    icon: `<svg viewBox="0 0 24 24" fill="currentColor" class="w-6 h-6"><path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347m-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884m8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z"/></svg>`,
  },
  {
    name: "Phone",
    url: "tel:+593984398542",
    icon: `<svg viewBox="0 0 24 24" fill="currentColor" class="w-6 h-6"><path d="M6.62 10.79c1.44 2.83 3.76 5.14 6.59 6.59l2.2-2.2c.27-.27.67-.36 1.02-.24 1.12.37 2.33.57 3.57.57.55 0 1 .45 1 1V20c0 .55-.45 1-1 1-9.39 0-17-7.61-17-17 0-.55.45-1 1-1h3.5c.55 0 1 .45 1 1 0 1.25.2 2.45.57 3.57.11.35.03.74-.25 1.02l-2.2 2.2z"/></svg>`,
  },
  {
    name: "CV",
    url: "/CV_Pablo.pdf",
    icon: `<svg viewBox="0 0 24 24" fill="currentColor" class="w-6 h-6"><path d="M14 2H6c-1.1 0-1.99.9-1.99 2L4 20c0 1.1.89 2 1.99 2H18c1.1 0 2-.9 2-2V8l-6-6zm2 16H8v-2h8v2zm0-4H8v-2h8v2zm-3-5V3.5L18.5 9H13z"/></svg>`,
  },
];
---

<section
  id="contact"
  class="relative bg-black py-16 md:py-20 px-8 md:px-24 font-['Inter'] overflow-hidden flex items-center"
>
  <!-- Background effects -->
  <div class="absolute inset-0 -z-10">
    <div
      class="absolute top-1/4 left-1/3 w-125 h-125 bg-purple-600/10 rounded-full blur-3xl"
    >
    </div>
    <div
      class="absolute bottom-1/4 right-1/3 w-100 h-100 bg-indigo-600/10 rounded-full blur-3xl"
    >
    </div>
    <div
      class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-150 h-150 bg-pink-600/5 rounded-full blur-3xl"
    >
    </div>
  </div>

  <!-- Animated grid lines -->
  <div class="absolute inset-0 -z-10 opacity-20">
    <div
      class="absolute inset-0"
      style="background-image: linear-gradient(rgba(139, 92, 246, 0.1) 1px, transparent 1px), linear-gradient(90deg, rgba(139, 92, 246, 0.1) 1px, transparent 1px); background-size: 50px 50px;"
    >
    </div>
  </div>

  <div class="max-w-6xl mx-auto w-full">
    <!-- Header -->
    <div class="mb-10 text-center">
      <span
        data-contact-animate
        class="inline-block text-xs tracking-[0.3em] uppercase text-purple-400 mb-3 opacity-0"
      >
        Get In Touch
      </span>
      <h2
        data-contact-animate
        class="text-3xl md:text-4xl lg:text-5xl font-black text-white mb-4 opacity-0"
      >
        Let's work
        <span
          class="bg-linear-to-r from-indigo-400 via-purple-500 to-pink-500 bg-clip-text text-transparent"
        >
          together
        </span>
      </h2>
      <p
        data-contact-animate
        class="text-white/50 text-base md:text-lg max-w-xl mx-auto leading-relaxed opacity-0"
      >
        I'm always open to discussing new projects, creative ideas, or
        opportunities to be part of your vision.
      </p>
    </div>

    <!-- Two Column Layout -->
    <div
      data-contact-card
      class="relative grid md:grid-cols-2 gap-6 md:gap-8 p-6 md:p-8 rounded-2xl border border-white/10 bg-white/5 backdrop-blur-sm overflow-hidden opacity-0"
    >
      <!-- Gradient border effect -->
      <div
        class="absolute inset-0 rounded-3xl opacity-0 hover:opacity-100 transition-opacity duration-500"
        style="background: linear-gradient(135deg, rgba(99, 102, 241, 0.1), rgba(168, 85, 247, 0.1), rgba(236, 72, 153, 0.1)); pointer-events: none;"
      >
      </div>

      <!-- Left Column - Contact Info -->
      <div class="flex flex-col justify-center text-left space-y-5">
        <div>
          <h3 class="text-xl md:text-2xl font-bold text-white mb-2">
            Let's talk about your project
          </h3>
          <p class="text-white/50 text-sm leading-relaxed">
            Fill out the form and I'll get back to you as soon as possible.
          </p>
        </div>

        <!-- Direct Email -->
        <div class="space-y-1">
          <p class="text-white/40 text-xs uppercase tracking-widest">Email</p>
          <a
            href="mailto:monteropablo511@gmail.com"
            data-contact-email
            class="inline-block text-lg font-bold text-white hover:text-purple-400 transition-colors duration-300"
          >
            monteropablo511@gmail.com
          </a>
        </div>

        <!-- Divider -->
        <div
          data-contact-divider
          class="w-16 h-px bg-linear-to-r from-purple-500 to-transparent opacity-0"
        >
        </div>

        <!-- Social Links -->
        <div>
          <p class="text-white/40 text-xs uppercase tracking-widest mb-3">
            Find me on
          </p>
          <div class="flex flex-wrap gap-2 md:gap-3">
            {
              socialLinks.map((social, index) => (
                <a
                  href={social.url}
                  target="_blank"
                  rel="noopener noreferrer"
                  data-contact-social
                  class="group relative p-2 md:p-2.5 rounded-lg border border-white/10 bg-white/5 hover:bg-white/10 hover:border-purple-500/50 transition-all duration-300 opacity-0"
                  style={`--delay: ${index * 0.1}s`}
                  aria-label={social.name}
                >
                  <div
                    class="text-white/60 group-hover:text-purple-400 transition-colors duration-300 [&>svg]:w-5 [&>svg]:h-5 md:[&>svg]:w-6 md:[&>svg]:h-6"
                    set:html={social.icon}
                  />
                  <span class="absolute -bottom-7 left-1/2 -translate-x-1/2 text-[10px] md:text-xs text-white/40 opacity-0 group-hover:opacity-100 transition-opacity duration-300 whitespace-nowrap">
                    {social.name}
                  </span>
                </a>
              ))
            }
          </div>
        </div>
      </div>

      <!-- Right Column - Contact Form -->
      <form
        id="contact-form"
        data-contact-form
        action="https://api.web3forms.com/submit"
        method="POST"
        class="space-y-4 text-left opacity-0"
      >
        <!-- Web3Forms Access Key -->
        <input
          type="hidden"
          name="access_key"
          value="a7cf6273-1e68-4538-9f34-12315cb198b2"
        />
        <!-- Redirect URL after submission (optional) -->
        <input type="hidden" name="redirect" value="" />
        <!-- Name & Email Fields -->
        <div class="grid grid-cols-2 gap-3">
          <div class="space-y-1">
            <label
              for="name"
              class="block text-white/60 text-xs uppercase tracking-widest"
            >
              Name
            </label>
            <input
              type="text"
              id="name"
              name="name"
              required
              placeholder="Your name"
              class="w-full px-3 py-2.5 bg-white/5 border border-white/10 rounded-lg text-white text-sm placeholder-white/30 focus:outline-none focus:border-purple-500/50 focus:bg-white/10 transition-all duration-300"
            />
          </div>

          <div class="space-y-1">
            <label
              for="email"
              class="block text-white/60 text-xs uppercase tracking-widest"
            >
              Email
            </label>
            <input
              type="email"
              id="email"
              name="email"
              required
              placeholder="your@email.com"
              class="w-full px-3 py-2.5 bg-white/5 border border-white/10 rounded-lg text-white text-sm placeholder-white/30 focus:outline-none focus:border-purple-500/50 focus:bg-white/10 transition-all duration-300"
            />
          </div>
        </div>

        <!-- Subject Field -->
        <div class="space-y-1">
          <label
            for="subject"
            class="block text-white/60 text-xs uppercase tracking-widest"
          >
            Subject
          </label>
          <input
            type="text"
            id="subject"
            name="subject"
            required
            placeholder="What's this about?"
            class="w-full px-3 py-2.5 bg-white/5 border border-white/10 rounded-lg text-white text-sm placeholder-white/30 focus:outline-none focus:border-purple-500/50 focus:bg-white/10 transition-all duration-300"
          />
        </div>

        <!-- Message Field -->
        <div class="space-y-1">
          <label
            for="message"
            class="block text-white/60 text-xs uppercase tracking-widest"
          >
            Message
          </label>
          <textarea
            id="message"
            name="message"
            required
            rows="3"
            placeholder="Tell me about your project..."
            class="w-full px-3 py-2.5 bg-white/5 border border-white/10 rounded-lg text-white text-sm placeholder-white/30 focus:outline-none focus:border-purple-500/50 focus:bg-white/10 transition-all duration-300 resize-none"
          ></textarea>
        </div>

        <!-- Submit Button -->
        <button
          type="submit"
          data-submit-btn
          class="group relative w-full py-3 px-6 rounded-lg bg-linear-to-r from-indigo-500 via-purple-500 to-pink-500 text-white font-semibold text-sm overflow-hidden transition-all duration-300 hover:shadow-lg hover:shadow-purple-500/25"
        >
          <span class="relative z-10 flex items-center justify-center gap-2">
            <span>Send Message</span>
            <svg
              class="w-4 h-4 transform group-hover:translate-x-1 transition-transform duration-300"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
            </svg>
          </span>
          <div
            class="absolute inset-0 bg-linear-to-r from-pink-500 via-purple-500 to-indigo-500 opacity-0 group-hover:opacity-100 transition-opacity duration-300"
          >
          </div>
        </button>

        <!-- Status Message -->
        <div
          id="form-status"
          class="hidden text-center py-2 rounded-lg text-sm"
        >
        </div>
      </form>
    </div>

    <!-- Footer text -->
    <p
      data-contact-footer
      class="mt-10 text-white/30 text-sm opacity-0 text-center"
    >
      © 2026 Pablo Montero. Built with
      <span class="text-purple-400">Astro</span> &
      <span class="text-indigo-400">React</span>
    </p>
  </div>

  <!-- Floating elements -->
  <div
    data-floating-element
    class="absolute top-20 left-10 w-2 h-2 bg-purple-500 rounded-full opacity-0"
  >
  </div>
  <div
    data-floating-element
    class="absolute top-40 right-20 w-3 h-3 bg-indigo-500 rounded-full opacity-0"
  >
  </div>
  <div
    data-floating-element
    class="absolute bottom-32 left-1/4 w-2 h-2 bg-pink-500 rounded-full opacity-0"
  >
  </div>
  <div
    data-floating-element
    class="absolute bottom-20 right-1/3 w-4 h-4 bg-purple-400/50 rounded-full opacity-0"
  >
  </div>
</section>

<script>
  import gsap from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  const initContactAnimations = () => {
    const contactSection = document.getElementById("contact");
    const contactElements = document.querySelectorAll("[data-contact-animate]");
    const contactCard = document.querySelector("[data-contact-card]");
    const contactForm = document.querySelector("[data-contact-form]");
    const contactEmail = document.querySelector("[data-contact-email]");
    const contactDivider = document.querySelector("[data-contact-divider]");
    const contactSocials = document.querySelectorAll("[data-contact-social]");
    const contactFooter = document.querySelector("[data-contact-footer]");
    const floatingElements = document.querySelectorAll(
      "[data-floating-element]",
    );
    const submitBtn = document.querySelector("[data-submit-btn]");

    if (!contactSection) return;

    // Main timeline
    const tl = gsap.timeline({
      scrollTrigger: {
        trigger: contactSection,
        start: "top 70%",
      },
    });

    // Animate header elements
    if (contactElements.length > 0) {
      tl.fromTo(
        contactElements,
        {
          opacity: 0,
          y: 50,
          filter: "blur(10px)",
        },
        {
          opacity: 1,
          y: 0,
          filter: "blur(0px)",
          duration: 0.8,
          stagger: 0.15,
          ease: "power3.out",
        },
      );
    }

    // Animate contact card
    if (contactCard) {
      tl.fromTo(
        contactCard,
        {
          opacity: 0,
          y: 60,
          scale: 0.95,
        },
        {
          opacity: 1,
          y: 0,
          scale: 1,
          duration: 0.8,
          ease: "power3.out",
        },
        "-=0.4",
      );
    }

    // Animate form
    if (contactForm) {
      tl.fromTo(
        contactForm,
        {
          opacity: 0,
          y: 30,
        },
        {
          opacity: 1,
          y: 0,
          duration: 0.6,
          ease: "power2.out",
        },
        "-=0.3",
      );

      // Animate form inputs with stagger
      const formInputs = contactForm.querySelectorAll("input, textarea");
      tl.fromTo(
        formInputs,
        {
          opacity: 0,
          x: -20,
        },
        {
          opacity: 1,
          x: 0,
          duration: 0.4,
          stagger: 0.1,
          ease: "power2.out",
        },
        "-=0.4",
      );
    }

    // Animate divider
    if (contactDivider) {
      tl.fromTo(
        contactDivider,
        {
          opacity: 0,
          scaleX: 0,
        },
        {
          opacity: 1,
          scaleX: 1,
          duration: 0.6,
          ease: "power2.out",
        },
        "-=0.3",
      );
    }

    // Animate email link
    if (contactEmail) {
      tl.fromTo(
        contactEmail,
        {
          opacity: 0,
          y: 20,
        },
        {
          opacity: 1,
          y: 0,
          duration: 0.6,
          ease: "power2.out",
        },
        "-=0.3",
      );
    }

    // Animate social links
    if (contactSocials.length > 0) {
      tl.fromTo(
        contactSocials,
        {
          opacity: 0,
          y: 30,
          scale: 0.8,
        },
        {
          opacity: 1,
          y: 0,
          scale: 1,
          duration: 0.5,
          stagger: 0.1,
          ease: "back.out(1.7)",
        },
        "-=0.2",
      );
    }

    // Animate footer
    if (contactFooter) {
      tl.fromTo(
        contactFooter,
        {
          opacity: 0,
          y: 20,
        },
        {
          opacity: 1,
          y: 0,
          duration: 0.6,
          ease: "power2.out",
        },
        "-=0.2",
      );
    }

    // Animate floating elements
    if (floatingElements.length > 0) {
      floatingElements.forEach((el, index) => {
        // Initial fade in
        gsap.fromTo(
          el,
          {
            opacity: 0,
            scale: 0,
          },
          {
            opacity: 0.6,
            scale: 1,
            duration: 0.8,
            delay: 1 + index * 0.2,
            ease: "elastic.out(1, 0.5)",
            scrollTrigger: {
              trigger: contactSection,
              start: "top 70%",
            },
          },
        );

        // Continuous floating animation
        gsap.to(el, {
          y: "random(-30, 30)",
          x: "random(-20, 20)",
          duration: "random(3, 5)",
          repeat: -1,
          yoyo: true,
          ease: "sine.inOut",
          delay: index * 0.5,
        });
      });
    }

    // Hover effect for email
    if (contactEmail) {
      contactEmail.addEventListener("mouseenter", () => {
        gsap.to(contactEmail, {
          scale: 1.05,
          duration: 0.3,
          ease: "power2.out",
        });
      });

      contactEmail.addEventListener("mouseleave", () => {
        gsap.to(contactEmail, {
          scale: 1,
          duration: 0.3,
          ease: "power2.out",
        });
      });
    }

    // Form submission handling
    const form = document.getElementById("contact-form") as HTMLFormElement;
    const formStatus = document.getElementById("form-status");

    if (form && formStatus) {
      form.addEventListener("submit", async (e) => {
        e.preventDefault();

        const submitButton = form.querySelector(
          "[data-submit-btn]",
        ) as HTMLButtonElement;
        const originalText = submitButton.innerHTML;

        // Show loading state
        submitButton.disabled = true;
        submitButton.innerHTML = `
          <span class="relative z-10 flex items-center justify-center gap-2">
            <svg class="w-5 h-5 animate-spin" fill="none" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
            <span>Sending...</span>
          </span>
        `;

        // Get form data
        const formData = new FormData(form);

        try {
          // Send to Web3Forms
          const response = await fetch("https://api.web3forms.com/submit", {
            method: "POST",
            body: formData,
          });

          const result = await response.json();

          if (result.success) {
            // Show success message
            formStatus.className =
              "text-center py-3 rounded-xl bg-green-500/20 text-green-400 border border-green-500/30";
            formStatus.textContent =
              "¡Message sent successfully! I'll get back to you soon.";
            formStatus.classList.remove("hidden");

            // Reset form
            form.reset();
          } else {
            // Show error message
            formStatus.className =
              "text-center py-3 rounded-xl bg-red-500/20 text-red-400 border border-red-500/30";
            formStatus.textContent = "Something went wrong. Please try again.";
            formStatus.classList.remove("hidden");
          }
        } catch (error) {
          // Show error message
          formStatus.className =
            "text-center py-3 rounded-xl bg-red-500/20 text-red-400 border border-red-500/30";
          formStatus.textContent = "Connection error. Please try again later.";
          formStatus.classList.remove("hidden");
        }

        // Animate status message
        gsap.fromTo(
          formStatus,
          { opacity: 0, y: -10 },
          { opacity: 1, y: 0, duration: 0.3 },
        );

        // Reset button
        submitButton.disabled = false;
        submitButton.innerHTML = originalText;

        // Hide status after delay
        setTimeout(() => {
          gsap.to(formStatus, {
            opacity: 0,
            duration: 0.3,
            onComplete: () => {
              formStatus.classList.add("hidden");
            },
          });
        }, 5000);
      });

      // Input focus animations
      const inputs = form.querySelectorAll("input, textarea");
      inputs.forEach((input) => {
        input.addEventListener("focus", () => {
          gsap.to(input, {
            scale: 1.02,
            duration: 0.2,
            ease: "power2.out",
          });
        });

        input.addEventListener("blur", () => {
          gsap.to(input, {
            scale: 1,
            duration: 0.2,
            ease: "power2.out",
          });
        });
      });
    }

    // Parallax effect on scroll
    gsap.to(contactSection.querySelector(".absolute.top-1\\/4"), {
      y: -100,
      scrollTrigger: {
        trigger: contactSection,
        start: "top bottom",
        end: "bottom top",
        scrub: 1,
      },
    });

    gsap.to(contactSection.querySelector(".absolute.bottom-1\\/4"), {
      y: 100,
      scrollTrigger: {
        trigger: contactSection,
        start: "top bottom",
        end: "bottom top",
        scrub: 1,
      },
    });
  };

  // Initialize when DOM is ready
  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", initContactAnimations);
  } else {
    initContactAnimations();
  }
</script>

<style>
  #contact a {
    position: relative;
  }

  #contact a::after {
    content: "";
    position: absolute;
    bottom: -2px;
    left: 0;
    width: 0;
    height: 2px;
    background: linear-gradient(90deg, #818cf8, #a855f7, #ec4899);
    transition: width 0.3s ease;
  }

  #contact a[data-contact-email]:hover::after {
    width: 100%;
  }

  [data-contact-social] {
    transform-origin: center;
  }

  [data-contact-social]:hover {
    transform: translateY(-4px);
  }

  /* Form styles */
  #contact-form input,
  #contact-form textarea {
    transform-origin: center;
  }

  #contact-form input:focus,
  #contact-form textarea:focus {
    box-shadow: 0 0 20px rgba(168, 85, 247, 0.15);
  }

  #contact-form input::placeholder,
  #contact-form textarea::placeholder {
    transition: opacity 0.3s ease;
  }

  #contact-form input:focus::placeholder,
  #contact-form textarea:focus::placeholder {
    opacity: 0.5;
  }

  [data-submit-btn] {
    transform-origin: center;
  }

  [data-submit-btn]:active {
    transform: scale(0.98);
  }

  [data-submit-btn]:disabled {
    cursor: not-allowed;
    opacity: 0.7;
  }

  /* Gradient border on focus */
  #contact-form input:focus,
  #contact-form textarea:focus {
    border-color: transparent;
    background-image:
      linear-gradient(rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0.1)),
      linear-gradient(135deg, #6366f1, #a855f7, #ec4899);
    background-origin: border-box;
    background-clip: padding-box, border-box;
  }
</style>
